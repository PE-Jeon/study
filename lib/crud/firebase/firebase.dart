import 'package:flutter/material.dart';

class FirebaseIO extends StatefulWidget {
  @override
  _FirebaseIOState createState() => _FirebaseIOState();
}

class _FirebaseIOState extends State<FirebaseIO> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Firebase IO Page"),
      ),
    );
  }
}
